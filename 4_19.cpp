// (a)
// ptr != 0 && *ptr++
// Tbh I can't figure out what is supposed to happen here
// (ptr != 0) && *ptr++ // maybe like that
//
// (b)
// ival + 1 && ival
//
// (c)
// vec[ival + 1] <= vec[ival]
